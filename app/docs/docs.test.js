'use strict';

describe('app.docs module', function() {

	beforeEach(module('app.docs'));

	describe('docs controller', function(){

		it('should ....', inject(function($controller) {
			//spec body
			var docsController = $controller('DocsController');
			expect(docsController).toBeDefined();
		}));

	});
});