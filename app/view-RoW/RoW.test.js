'use strict';

describe('app.rod-of-wonder module', function() {

	beforeEach(module('app.rod-of-wonder'));

	describe('rod-of-wonder controller', function(){

		it('should ....', inject(function($controller) {
			//spec body
			var rowController = $controller('RoWController');
			expect(rowController).toBeDefined();
		}));

	});
});